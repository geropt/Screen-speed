# 16 MB layout. Two 3 MB app slots so the firmware can be updated over WiFi
# (a single `factory` partition makes OTA impossible), plus a data partition for
# whatever needs to live off the SD card. `nvs` keeps its original offset and
# size so anything already stored there survives the migration.
#
# Sums to exactly 0x1000000 = 16 MB. App partitions must stay 64 KB aligned.
#
# Name,      Type, SubType, Offset,      Size
nvs,         data, nvs,     0x9000,      0x6000
otadata,     data, ota,     0xF000,      0x2000
phy_init,    data, phy,     0x11000,     0x1000
ota_0,       app,  ota_0,   0x20000,     0x300000
ota_1,       app,  ota_1,   0x320000,    0x300000
storage,     data, fat,     0x620000,    0x9E0000
